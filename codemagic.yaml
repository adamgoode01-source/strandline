# Codemagic CI/CD - builds Strandline for iOS on a macOS machine and ships to TestFlight.
#
# Before the first run, one thing must exist in the Codemagic UI (it cannot live here):
#   Team settings > Integrations > Apple Developer Portal -> add your App Store Connect
#   API key (Issuer ID, Key ID, .p8 file). Name that integration EXACTLY:
#
#       JC Concrete App Store Connect
#
#   It is referenced by name under `integrations` below. A mismatch is the most
#   common first-build failure.
#
# Docs: https://docs.codemagic.io/yaml-quick-start/building-a-native-ios-app/

workflows:
  ios-testflight:
    name: Strandline iOS - TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 45

    integrations:
      # Name of the Apple Developer Portal integration created in Codemagic team settings.
      app_store_connect: JC Concrete App Store Connect

    environment:
      vars:
        BUNDLE_ID: "com.jcconcrete.strandline"
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
      node: 24
      xcode: latest

      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.jcconcrete.strandline

    cache:
      cache_paths:
        - ~/.npm

    scripts:
      - name: Install npm dependencies
        script: npm ci

      - name: Sync web assets into the iOS project
        script: npx cap sync ios

      - name: Set the build number
        script: |
          cd "$CM_BUILD_DIR/ios/App"
          # Codemagic's per-project counter guarantees a build number App Store
          # Connect has not seen before, which it rejects duplicates of.
          agvtool new-version -all "$(($BUILD_NUMBER + 1))"

      - name: Fetch signing certificates and profiles
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE --create
          keychain add-certificates
          xcode-project use-profiles

      - name: Build the ipa
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME" \
            --config Release

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
        # Once the internal tester group exists in App Store Connect, uncomment
        # this and the build will be handed to it automatically. The name must
        # match the group exactly. Until then the build simply lands in
        # TestFlight and you assign it by hand.
        # beta_groups:
        #   - JC Concrete Field

      email:
        recipients:
          - agoode@jcconcretellc.com
        notify:
          success: true
          failure: true
